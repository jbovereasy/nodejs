<h1>Results</h1>

<% data["search"].forEach(function(movie){ %>
    <ul>
        <li>
            <strong>
                <%= movie["Title"] %>
            </strong>
            - <%= movie["Year"] %>
        </li>
    </ul>
<% }); %>